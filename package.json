{
  "name": "@projectx/website",
  "version": "1.0.0",
  "private": true,
  "description": "Presentation website for ProjectX Protocol — the no-loss prize pool deployed on Sui mainnet.",
  "scripts": {
    "dev": "next dev -p 4000",
    "build": "next build",
    "start": "next start -p 4000",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run guardrails && npm run verify",
    "guardrails": "python3 scripts/guardrails.py",
    "test": "node --test \"lib/**/*.test.ts\"",
    "verify": "node scripts/verify-contrast.mjs && node scripts/verify-claims.mjs && node scripts/verify-no-private-hosts.mjs && node scripts/verify-retired-hosts.mjs && node scripts/verify-suins-prices.mjs && node scripts/verify-verify-pricing.mjs"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "clsx": "^2.1.1",
    "next": "^16.3.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwind-merge": "^2.6.1"
  },
  "devDependencies": {
    "@mysten/sui": "^2.24.0",
    "@mysten/suins": "^1.2.14",
    "@types/node": "^26.1.2",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.6.3"
  }
}
